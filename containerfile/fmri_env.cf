# This script extends the base image for comp-env with additional packages. 

FROM base

ARG SH_RUN=fmri_env.sh
ARG TMPDIR=/tmp

COPY run/$SH_RUN $TMPDIR
COPY run/requirements/fmri_env.txt $TMPDIR

RUN \
    bash $TMPDIR/$SH_RUN    && \
    rm $TMPDIR/$SH_RUN